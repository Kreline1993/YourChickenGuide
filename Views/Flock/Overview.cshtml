@model YourChickenGuide.Models.ViewModels.ChickenOverviewVm
@{
    ViewData["Title"] = "Overview";
    string Toggle(string column) =>
        (Model.SortBy == column && Model.SortDir == "asc") ? "desc" : "asc";
}

<div class="text-center">  
    <h1 class="display-4">Overview</h1>  
 
    <a class="btn btn-primary" asp-controller="Flock" asp-action="AddChicken">Add</a>  

</div>  

<div class="text-center">
    <form method="get" class="row g-3 align-items-end mb-3">
        <div class="col-md-2">
            <label class="form-label">Breed</label>
            <select name="breed" class="form-control" asp-items="Model.Breeds"></select>
        </div>

        <div class="col-md-2">
            <label class="form-label">Sex</label>
            <select name="sex" class="form-control">
                <option value="">Any</option>
                <option value="Female" selected="@(Model.Sex == "Female")">Female</option>
                <option value="Male" selected="@(Model.Sex == "Male")">Male</option>
                <option value="Unknown" selected="@(Model.Sex == "Unknown")">Unknown</option>
            </select>
        </div>

        <div class="col-md-2">
            <label class="form-label">Status</label>
            <select name="status" class="form-control" asp-items="Model.Statuses">
                <!-- Model.Statuses should include: "Any status" (blank) + your list -->
            </select>
        </div>

        <div class="col-md-2">
            <label class="form-label">Mother</label>
            <select name="motherId" class="form-control" asp-items="Model.Mothers"></select>
        </div>

        <div class="col-md-2">
            <label class="form-label">Father</label>
            <select name="fatherId" class="form-control" asp-items="Model.Fathers"></select>
        </div>

        <div class="col-md-2">
            <label class="form-label">Search</label>
            <input name="search" value="@Model.Search" class="form-control" placeholder="Legband, color, notes..." />
        </div>

        <div class="col-12 text-end">
            <button class="btn btn-primary">Filter</button>
            <a class="btn btn-secondary" href="@Url.Action("Overview", "Flock")">Reset</a>
        </div>
    </form>
    <h2>Current Chickens</h2>
        <table class="table table-striped table-hover">
            <thead>
                <tr>
                    <th>
                        <a asp-action="Overview"
                           asp-route-breed="@Model.Breed"
                           asp-route-sex="@Model.Sex"
                           asp-route-motherId="@Model.MotherId"
                           asp-route-fatherId="@Model.FatherId"
                           asp-route-onlyActive="@Model.Status"
                           asp-route-search="@Model.Search"
                           asp-route-sortBy="legband"
                           asp-route-sortDir="@Toggle("legband")">
                            Legband
                        </a>
                    </th>
                    <th>
                        <a asp-action="Overview" asp-route-breed="@Model.Breed" asp-route-sex="@Model.Sex"
                           asp-route-motherId="@Model.MotherId" asp-route-fatherId="@Model.FatherId"
                           asp-route-onlyActive="@Model.Status" asp-route-search="@Model.Search"
                           asp-route-sortBy="hatch" asp-route-sortDir="@Toggle("hatch")">
                            Hatch Date
                        </a>
                    </th>
                    <th>
                        <a asp-action="Overview" asp-route-breed="@Model.Breed" asp-route-sex="@Model.Sex"
                           asp-route-motherId="@Model.MotherId" asp-route-fatherId="@Model.FatherId"
                           asp-route-onlyActive="@Model.Status" asp-route-search="@Model.Search"
                           asp-route-sortBy="breed" asp-route-sortDir="@Toggle("breed")">
                            Breed
                        </a>
                    </th>
                    <th>
                        <a asp-action="Overview" asp-route-breed="@Model.Breed" asp-route-sex="@Model.Sex"
                           asp-route-motherId="@Model.MotherId" asp-route-fatherId="@Model.FatherId"
                           asp-route-onlyActive="@Model.Status" asp-route-search="@Model.Search"
                           asp-route-sortBy="sex" asp-route-sortDir="@Toggle("sex")">
                            Sex
                        </a>
                    </th>
                    <th>
                        <a asp-action="Overview" asp-route-breed="@Model.Breed" asp-route-sex="@Model.Sex"
                           asp-route-motherId="@Model.MotherId" asp-route-fatherId="@Model.FatherId"
                           asp-route-onlyActive="@Model.Status" asp-route-search="@Model.Search"
                           asp-route-sortBy="status" asp-route-sortDir="@Toggle("status")">
                            Status
                        </a>
                    </th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                @foreach (var c in Model.Chickens)
                {
                    <tr>
                        <td>@c.Legband_Id</td>
                        <td>@c.HatchDate.ToString("yyyy-MM-dd")</td>
                        <td>@c.Breed</td>
                        <td>@c.Sex</td>
                        <td>@c.Status</td>
                        <td>
                            <a class="btn btn-sm btn-outline-info"
                               asp-action="ViewChicken" asp-route-id="@c.Id">View</a>
                            <a class="btn btn-sm btn-outline-primary"
                               asp-action="EditExistingChicken" asp-route-id="@c.Id">Edit</a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    }
</div>